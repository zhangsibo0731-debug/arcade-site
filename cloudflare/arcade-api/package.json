{
  "name": "arcade-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "db:local": "wrangler d1 migrations apply arcade-db --local",
    "db:remote": "wrangler d1 migrations apply arcade-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
